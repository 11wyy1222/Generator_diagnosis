# 发电机轴承磨损异常检测模型实施方案（可执行版）

## 1. 执行目标

本文档对应《发电机轴承磨损异常检测算法设计（单波形版）》，用于直接指导数据整理、训练、验证、测试和交付。

模型每次输入一个测点的一条完整振动波形，输出：

```json
{
  "sample_id": "sample_001",
  "abnormal_probability": 0.92
}
```

`abnormal_probability` 是当前波形相对于正常参考分布的归一化异常程度，不是真实故障概率。模型不输出故障部位、正常/故障结论、告警等级、阈值判断或 `confidence`。

本模型只用确认正常波形训练。异常时间范围不生成逐波形训练标签，不参与编码器、缩放参数、正常中心、协方差、分数分布或融合权重拟合。

---

## 2. 实际使用数据

### 2.1 数据对象

| 对象 ID | 机型 | 项目与机组 | 本模型用途 | 异常时间范围 | 正常时间范围 | RPM |
|---|---|---|---|---|---|---|
| `SD_DEV_23` | 半直驱 | 阳江沙扒，23#，MySE6.45-180/105 | 正常训练、正常验证、异常验证 | 2023-05-14 00:00:00 ～ 2024-01-06 04:59:19 | 2024-03-17 11:54:39 ～ 2024-04-17 11:54:39 | 180～350 |
| `SD_DEV_38` | 半直驱 | 阳江沙扒，38#，MySE6.45-180/105 | 新增完整异常验证 | 2023-01-01 ～ 2025-09-15（完整异常范围） | 无 | 180～350 |
| `SD_DEV_22` | 半直驱 | 阳江沙扒，22#，MySE6.45-180/105 | 新增正常训练、正常验证 | 无 | 正常文件夹内全部准入波形（不设时间范围） | 180～350 |
| `SD_TEST_13` | 半直驱 | 阳江沙扒，13#，MySE6.45-180/105 | 跨机组正常+异常测试 | 2023-01-03 00:00:00 ～ 2024-01-05 06:39:15 | 2024-01-28 03:01:42 ～ 2024-02-28 03:01:42 | 180～350 |
| `DFIG_DEV_41` | 双馈 | 乳山半岛南海上风电 U 厂址一期，41#，MySE8.5-230/131 | 正常训练、正常验证、异常验证 | 2024-01-03 00:00:00 ～ 2026-05-15 05:36:00 | 2026-05-16 21:36:00 ～ 2026-06-16 21:36:00 | 600～1200 |
| `DFIG_TEST_F14` | 双馈 | 甘肃武威天祝，f14，MySE5.0-200/115 | 测点域外异常探索 | 2026-03-25 18:30:45 ～ 2026-07-26 08:49:15 | 无 | 1000～1900 |

半直驱 22#、23#、38# 和13#属于同一个阳江沙扒项目。因此 `SD_TEST_13` 是相对于半直驱开发对象的同项目跨机组测试，不是跨项目测试。

`SD_DEV_38` 的已知事件为发电机非驱动端轴承外环故障，轴承型号 `NU10/560M`，四部件阶次为滚动体 5.3364、保持架 0.4536、外环 12.6996、内环 15.3004；`fault_event_id` 记为 `SD_DEV_38_event_001`。38#完整异常范围只用于开发异常验证，不参与编码器、缩放参数、正常中心、协方差或正常分数分布拟合；它可以与23#异常验证一起用于开发阶段的单分支/固定融合方案比较，但不得单独据此降低评估阈值。

`SD_DEV_22` 不执行正常时间范围过滤：直接扫描配置的22#正常文件夹，文件夹内通过准入检查的全部波形作为确认正常数据。实际最早/最晚采集时间只写入数据快照，不作为纳入边界。经业务确认，其理论故障阶次与38#相同：滚动体5.3364、保持架0.4536、外环12.6996、内环15.3004。

### 2.2 测点范围

- `SD_DEV_23`：4 个正式测点为 `12点`、`3点`、`6点`、`9点`，分别建立 4 个正常范围和 4 个异常范围。
- `SD_DEV_38`：实测新增文件名不含测点字段，因此 `sensor_position` 由业务确认的四个独立来源目录映射为 `12点/3点/6点/9点`；目录不得合并，每个实际出现的测点建立完整异常范围，全部共享 `SD_DEV_38_event_001`。
- `SD_DEV_22`：新增正常文件的 `sensor_position` 从文件名解析，取值必须属于 `12点/3点/6点/9点`；每个实际出现的测点建立正常文件夹来源记录，不设时间范围。不得假设四个测点均存在。
- `SD_TEST_13`：同一测点提供 1 个完整异常范围和 1 个维修后正常范围；具体 `sensor_position` 从文件名解析，取值必须属于 `12点/3点/6点/9点`，且正常、异常文件解析结果必须一致。
- `DFIG_DEV_41`：1 个非驱动端测点，建立 1 个正常范围和 1 个异常范围。
- `DFIG_TEST_F14`：1 个驱动端测点，只有 1 个异常范围。

半直驱开发侧同一机组的多个测点范围属于同一持续故障过程时，只计为 1 个独立故障事件，不能按测点数扩大案例数。新增后半直驱开发侧最多有 2 个独立异常事件：`SD_DEV_23_event_001` 和 `SD_DEV_38_event_001`。

### 2.3 测试有效性限制

正常参考按 `machine_type + sensor_position + rpm_bin` 管理：

- 半直驱测试测点必须与开发侧同名测点位置匹配，否则只作为探索性案例。
- 半直驱正常参考可在完成准入后由23#与22#的确认正常数据共同拟合，但必须保留 `object_id` 分组统计，并检查22#是否改变23#正常验证尾部；38#异常数据不进入任何正常参考拟合。
- 双馈开发正常数据是 `non_drive_end`，测试数据是 `drive_end`，当前没有匹配的驱动端正常参考。因此 `DFIG_TEST_F14` 不能作为本模型的正式外部测试，只能标记为 `sensor_position_ood_exploration`。
- 双馈测试的 1000～1200 RPM 属于训练范围交集；大于 1200 RPM 的数据另记为 `rpm_ood_test`。

---

## 3. 数据清单

### 3.1 目录

```text
dataset_root/
├─ waveforms/
├─ manifests/
│  ├─ samples.jsonl
│  ├─ ranges.jsonl
│  ├─ rejected_samples.jsonl
│  └─ data_snapshot.json
├─ splits/
│  └─ anomaly_detection_split.jsonl
└─ features/
   └─ mechanism_features.parquet
```

### 3.2 单波形记录

```json
{
  "sample_id": "SD_DEV_23_p03_20230514001000",
  "object_id": "SD_DEV_23",
  "project_id": "S1-20180011",
  "turbine_id": "23",
  "machine_type": "semi_direct",
  "sensor_position": "3点",
  "waveform_path": "[path]",
  "acquisition_time": "2023-05-14 00:10:00",
  "sampling_rate_hz": 25600.0,
  "waveform_length": 256000,
  "rpm": 215.0,
  "rpm_source": "filename",
  "range_id": "SD_DEV_23_p03_abnormal",
  "fault_event_id": "SD_DEV_23_event_001"
}
```

单波形记录不保存由异常范围传播得到的逐波形标签。

#### 3.2.1 文件名解析与自动统计

去掉扩展名后，从文件名最右侧解析固定后缀：

```text
<设备与测点描述>_<fs>Hz_<rpm>_<YYYYMMDDhhmmss>
```

示例：

```text
广东能源尚洋湾海风场*23#*发电机3点径向_25600Hz_65.8824_20231004171053
```

解析结果：

```text
sensor_position = 3点
sampling_rate_hz = 25600
rpm = 65.8824
acquisition_time = 2023-10-04 17:10:53
```

实现时先按最右侧三个下划线字段解析 `fs`、RPM 和时间，再从前缀中的 `发电机(12点|3点|6点|9点)` 提取半直驱测点，避免设备或项目名称中的其他数字干扰。文件名不匹配、时间非法、测点出现零次或多次时拒绝样本。`waveform_length=N` 为波形文件第一列的实际长度，不从文件名推断。

清单生成后，按 `object_id + sensor_position + range_id` 自动计算波形文件数量；按 `acquisition_time` 排序后对相邻时间做差，输出采样间隔的最小值、中位数、P90、最大值、重复时间戳数量和明显断档数量。

### 3.3 范围记录

```json
{
  "range_id": "SD_DEV_23_p03_abnormal",
  "object_id": "SD_DEV_23",
  "machine_type": "semi_direct",
  "turbine_id": "23",
  "sensor_position": "3点",
  "range_start": "2023-05-14 00:00:00",
  "range_end": "2024-01-06 04:59:19",
  "is_observed_scope_abnormal": true,
  "label_source": "expert_review",
  "fault_event_id": "SD_DEV_23_event_001",
  "dataset_role": "development"
}
```

范围语义：

- `false`：范围内没有观察到异常波形，通过准入检查的波形可用于正常建模。
- `true`：范围内至少存在一条或一部分异常波形，不表示每条波形都异常。

正常范围统一取“正常数据上线时间起一个自然月”，`range_end` 使用第 2.1 节的明确时间；范围内实际最后一条有效波形时间另写入数据快照统计。每次重新导出必须生成新的 `data_snapshot_id`。

`SD_DEV_22` 是上述时间范围规则的明确例外：它按业务确认的正常文件夹整体纳入，不设置 `range_start/range_end`。对应范围记录增加：

```json
{
  "range_id": "SD_DEV_22_confirmed_normal_directory",
  "object_id": "SD_DEV_22",
  "machine_type": "semi_direct",
  "scope_source_type": "confirmed_normal_directory",
  "normal_directory_path": "[path]",
  "range_start": null,
  "range_end": null,
  "is_observed_scope_abnormal": false,
  "dataset_role": "development"
}
```

该对象实际首条和末条有效波形时间、文件总数、通过数和拒绝数写入数据快照，但不反向生成时间过滤边界。

---

## 4. 数据准入

波形必须同时满足：

1. 文件存在且能读取为一维有限数值数组；
2. `waveform_length=N` 读取每条波形第一列长度；同一 `object_id + sensor_position` 只保留一致的目标长度，不一致文件从建模数据集剔除并写入 `rejected_samples.jsonl`，原始源文件不覆盖；不同测点之间允许长度不同；
3. 采样率可解析且大于 0；
4. RPM 可解析并位于对象配置范围；
5. 数据点数能够执行 FFT；
6. 不存在严重削顶、饱和、长时间恒定或通信填充值；
7. 文件名解析出的时间、RPM、采样率和测点与对象配置、范围记录不存在冲突。

对 `SD_DEV_22`，第7项中的范围冲突检查替换为正常文件夹归属检查：文件必须位于冻结的正常目录下，且不得同时出现在任何异常对象或异常范围清单中。

失败样本写入 `rejected_samples.jsonl`，不得静默忽略，不记录 `signal_quality_level`。

数据构建后必须输出每个对象、测点和范围的原始数、通过数、拒绝数、采样率、波形长度、采样时长 `N/fs`、原始频率分辨率 `fs/N`、截止频率 `fs/2.56`、RPM 分布及时间块数量。

---

## 5. 数据切分

### 5.1 时间块

每个正常范围按时间排序：

1. 默认按自然日形成 `sample_group_id`；
2. 若相邻采样间隔中位数大于 1 天，则按连续采集批次分块；相邻时间差大于 `3 × 中位采样间隔` 时开始新块；
3. 不按单波形随机切分；
4. 同一原始波形的内部窗口不得跨集合；
5. 半直驱同一机组同一自然日或同一连续采集批次的 `12点/3点/6点/9点` 共享切分键和切分结果；
6. 每个正常范围至少需要 3 个非空时间块。

`SD_DEV_22` 正常文件夹内的全部准入波形同样按实际采集时间执行上述分块；四个测点在同一自然日或连续采集批次共享切分键。22#与23#属于不同对象，不共享 `sample_group_id`，但均可进入半直驱正常训练/验证集合。

### 5.2 分配

冻结 `split_seed: 2026`，在任何预处理参数、编码器或正常参考拟合前生成不可变切分：

| 数据 | 分配 |
|---|---|
| 开发正常范围约 70% 完整时间块 | 正常训练集 |
| 开发正常范围约 30% 完整时间块 | 正常验证集 |
| 开发异常范围全部有效波形 | 异常验证集 |
| `SD_DEV_38` 完整异常范围全部有效波形 | 新增独立事件异常验证集，不进入正常训练 |
| `SD_TEST_13` 正常范围全部有效波形 | 模型冻结后的跨机组正常测试 |
| `SD_TEST_13` 完整异常范围全部有效波形 | 模型冻结后的跨机组异常测试 |
| `DFIG_TEST_F14` 完整异常范围全部有效波形 | 模型冻结后的域外异常探索 |

正常范围按时间块数取 `n_val=max(1, round(0.3 × n_blocks))`，其余进入训练集，并保证至少 2 个训练块。随机单位只能是 `sample_group_id`，不能拆分单波形。切分后按 `sensor_position + rpm_bin` 检查训练/验证覆盖；若具有足够时间块的 RPM 分箱只出现在一侧，则保持相同 `split_seed`，改用完整时间块的分层随机划分。不得根据模型指标重复抽取或挑选切分。

上述开发正常范围包括23#正常月和22#正常文件夹。切分和报告必须保留 `object_id`，分别给出22#、23#的正常训练/验证数量及分数分布；不得先把两台机组的波形混合后按单波形随机切分。

半直驱同一日期或采集批次的 4 个测点共享切分结果，但不同测点的正常参考仍独立拟合。`split_manifest.jsonl` 至少保存 `sample_group_id`、`sensor_position`、`rpm_bin`、`dataset_split` 和 `split_seed`，所有模型与对比实验复用同一清单。

同一 `sensor_position + rpm_bin` 的正常参考可汇总22#和23#正常训练数据拟合，同时必须保存每个 `object_id` 的贡献数量。若22#测点无法与23#同名位置匹配，或轴承/采集配置不兼容，则22#对应数据只建立独立探索参考，不得直接混入正式参考。

异常验证范围不得切出训练样本。测试范围不得用于选择频带、网络结构、融合权重、RPM 分箱或 `evaluation_threshold`。

---

## 6. 信号预处理

### 6.1 时域

```text
完整波形 → 去均值 → 除以正常训练集绝对幅值全局 P99.5 → 时域编码器
```

禁止逐样本最大值归一化、标准化或总能量归一化。

不同测点之间 `N` 不一致时不为统一长度而降采样、截断或补零。数据加载器按 `waveform_length` 分桶组成批次，同一个批次内波形长度一致；共享时域编码器通过自适应分段池化把不同批次的卷积输出映射为相同维度。若同一测点内部出现多个 `N`，先检查采集配置和数据完整性，不自动混入训练。

### 6.2 普通频谱

```text
去均值波形 → Hann 窗 → 单边 FFT → 线性幅值
→ 使用该波形自己的 fs、N 计算原生 Hz 频率轴
→ 截取公共频带 → 按真实 Hz 坐标插值到统一 Hz 网格
```

### 6.3 包络频谱

```text
去均值波形 → 全频段 Hilbert 包络 → 去直流
→ Hann 窗 → 单边 FFT → 线性幅值
→ 使用该波形自己的 fs、N 计算原生 Hz 频率轴
→ 截取相同公共频带 → 插值到相同统一 Hz 网格
```

第一版不使用带通滤波、对数幅值、阶次谱、角域重采样或逐样本能量归一化。

### 6.4 统一 Hz 频率网格

当前各测点采样率 `fs` 接近，主要差异是采样点数 `N` 不一致。`N` 不一致意味着波形时长和 FFT 原始频率分辨率不同：

```text
duration_seconds = N / fs
source_delta_f_hz = fs / N
source_cutoff_hz = fs / 2.56
```


每个机型只使用**正常训练集**统计量建立一套统一网格：

```text
common_f_max_hz = min(
    business_f_max_hz,
    min(source_cutoff_hz of normal training samples)
)

common_delta_f_hz = max(
    source_delta_f_hz of normal training samples
)

common_frequency_axis = np.arange(
    0,
    common_f_max_hz + common_delta_f_hz / 2,
    common_delta_f_hz
)

frequency_bin_count = len(common_frequency_axis)
```

其中：

- `business_f_max_hz` 根据需要保留的轴承冲击、共振调制频带以及 1～5 阶故障频率和边带确定。
- `common_delta_f_hz` 取正常训练数据中最差、也就是最大的真实 `fs/N`，避免把较短波形插值到虚假的更细分辨率。
- `frequency_bin_count` 由 `common_f_max_hz/common_delta_f_hz` 得到。
- 网格一经开发训练阶段确定，正常验证、异常验证、测试和生产必须加载同一份 `frequency_grid.npy`，不得重新计算。

每条波形分别按自身 `fs` 和 `N` 生成原生频率轴，再按 Hz 插值：

```python
source_frequency = np.fft.rfftfreq(N, d=1.0 / fs)
source_cutoff_hz = fs / 2.56
keep = source_frequency <= source_cutoff_hz

source_frequency = source_frequency[keep]
fft_native = fft_native[keep]
envelope_native = envelope_native[keep]

if source_frequency[-1] < common_frequency_axis[-1]:
    raise ValueError("频谱没有覆盖训练阶段冻结的统一频率上限")

fft_uniform = np.interp(
    common_frequency_axis, source_frequency, fft_native
)
envelope_uniform = np.interp(
    common_frequency_axis, source_frequency, envelope_native
)
spectrum_input = np.stack([fft_uniform, envelope_uniform], axis=0)
```

最终输入形状为：

```text
[2, frequency_bin_count]
```

统一网格只服务于频谱 CNN。BPFO、BPFI、BSF、FTF 的峰值、谐波、背景和边带特征必须在插值前的原生 Hz 频率轴上提取，保留每条波形的真实 FFT 分辨率。

线性插值作为第一版基线。如果长波形从细网格压缩到较粗公共网格后出现明显窄峰衰减，再在相同验证切分上比较“目标频率区间最大值分箱”；未经对比不增加通道。

### 6.5 RPM

```text
rpm_scaled = (rpm - rpm_min) / (rpm_max - rpm_min)
```

半直驱使用 180～350；双馈开发模型使用 600～1200。RPM 只作为融合阶段标量，不设置独立编码器。

---

## 7. 模型结构

分别训练：

- `anomaly_semi_direct_v1`；
- `anomaly_dfig_v1`。

```yaml
time_encoder:
  type: multiscale_1d_cnn
  stem_channels: 32
  multiscale_kernels: [7, 15, 31]
  channels: [32, 64, 128]
  stride_per_block: 2
  padding: kernel_size_div_2_floor
  normalization: group_norm
  activation: gelu
  pool_segments: 8
  output_dim: 128

spectrum_encoder:
  input_channels: 2
  input_length: frequency_bin_count_from_frozen_grid
  channels: [32, 64, 128]
  kernels: [9, 7, 5]
  stride_per_block: 2
  padding: kernel_size_div_2_floor
  normalization: group_norm
  activation: gelu
  pool_segments: 8
  output_dim: 128

fusion:
  input: [z_raw, z_spectrum, rpm_scaled]
  hidden_dims: [128, 64]
  dropout: 0.2
  output_dim: 64
```

模型不输入 `machine_type`、`turbine_id` 或 `sensor_position`；这些字段用于选择模型和正常参考。

---

## 8. 正常表征训练

仅使用正常训练时间块，分两步执行：

1. 正常一致性预训练；
2. 冻结最优编码器并拟合正常中心和 Ledoit-Wolf 收缩协方差。

一致性视图只允许：

- 循环时间平移不超过波形长度 1%；
- 高斯噪声标准差不超过正常训练集全局波形标准差 0.5%。

不使用逐样本幅值缩放增强。

```text
L_normal = 1 - cosine_similarity(z_view1, z_view2)
         + 0.10 × variance_floor_penalty
         + 0.01 × off_diagonal_covariance_penalty
```

深度异常距离：

```text
deep_anomaly_score = (z_deep - mean)^T
                     × shrink_covariance_inverse
                     × (z_deep - mean)
```

训练配置：

```yaml
optimizer: AdamW
learning_rate: 0.001
weight_decay: 0.0001
batch_size: 32
max_epochs: 100
early_stopping_patience: 15
monitor: normal_validation_loss
random_seeds: [2026, 2027, 2028]
```

半直驱按 4 个测点和 RPM 分箱均衡采样；双馈按 RPM 分箱均衡采样。

---

## 9. 机理异常分数与双分支融合

### 9.1 机理分数

复用现有机理算法在每条波形原生 Hz 频率轴上得到的逐波形连续输出，包括候选峰值、峰值位置、背景中位数、峰背景比、有效谐波数量和谱间一致性。不得从为 CNN 生成的统一网格反向提取精细机理峰值。

不得使用：

- 现有布尔报警字段作为真实标签；
- 沿多条记录维度求和后的结果；
- 缺失证据填 0 冒充正常。

对连续证据按同机型、同测点位置和 RPM 分箱建立正常中位数/MAD参考，得到 `mechanism_anomaly_score`。

### 9.2 固定融合

`deep_anomaly_score` 和 `mechanism_anomaly_score` 分别使用正常训练经验分布转换为 `[0,1]` 百分位，再进行固定权重融合：

```text
abnormal_probability = Σ(valid_j × weight_j × percentile_j)
                       / Σ(valid_j × weight_j)
```

默认权重：

```yaml
deep: 0.625
mechanism: 0.375
```

默认权重保持深度表征为主、机理证据为辅，并通过开发验证比较是否保留。机理分支缺失时模型退化为仅深度异常分数；时域或频谱主输入无效时直接拒绝样本。

固定比较：

1. A 组：仅深度异常分数；
2. B 组：仅机理异常分数；
3. C 组：深度与机理固定权重融合。

融合权重只根据正常验证尾部和开发异常验证范围比较，测试集不参与调权。

---

## 10. 评估

### 10.1 开发验证

按机型、机组、测点、RPM 分箱和 `range_id` 报告：

- 正常训练、正常验证和异常验证分数分布；
- 正常验证 P50、P90、P95、P99；
- 异常范围分数相对匹配正常验证分布的百分位；
- 异常验证范围的异常范围阈值命中率（范围级弱标签 Recall）；
- 异常范围 Top-10%、Top-20% 波形清单；
- 专家对高分、低分和边界样本的抽样复核；
- 分支缺失和降级表现；
- 3 个随机种子的稳定性。

新增半直驱数据必须单独报告：22#正常训练/正常验证分数分布及其相对23#正常参考的位置；38#完整异常范围按 `fault_event_id`、测点、RPM分箱和时间阶段的分数分布及阈值命中率。23#与38#是两个独立异常事件，先分别报告，再给出事件等权汇总，不能按波形数量直接加权掩盖事件差异。

第一版按匹配的正常参考组确定离线评估阈值：

```text
evaluation_threshold = P95(abnormal_probability of matched normal validation group)
异常范围阈值命中率 = 异常范围内 abnormal_probability >= evaluation_threshold 的有效波形数 / 异常范围有效波形总数
```

模型和正常参考组确定后冻结 `evaluation_threshold`。外部测试只能复用，禁止根据测试结果调节；开发异常范围可用于比较模型，但不得为了提高命中率单独降低阈值。该阈值不写入对外输出，也不是生产告警阈值。

没有逐波形异常真值，不能报告可信的逐波形 Precision、Recall、F1 或混淆矩阵。异常范围阈值命中率在计算形式上等同于临时传播范围标签得到的 Recall，因此称为“范围级弱标签 Recall”，不能解释为逐波形真实 Recall。把范围标签临时下放计算的 AUC 只能称为范围分布可分性。

### 10.2 外部测试

测试报告：

- 有效数、拒绝数；
- 分数 P10、P25、P50、P75、P90；
- 相对开发正常验证分布的百分位；
- 使用开发阶段冻结的 `evaluation_threshold` 计算异常范围阈值命中率（范围级弱标签 Recall）；
- Top-N 波形及专家复核；
- RPM 和测点是否属于训练参考范围。

`SD_TEST_13` 同时具有维修后正常范围和完整异常范围，因此可报告正常范围高分率、特异度、异常范围阈值命中率（范围级弱标签 Recall），以及正常/异常范围来源的分数分布和范围来源可分性指标。由于没有逐波形人工真值，这些指标不能解释为真实逐波形诊断性能，范围级弱标签 Recall 也不能解释为逐波形真实 Recall。

`DFIG_TEST_F14` 仍只有异常范围，只报告异常侧分数分布、异常范围阈值命中率（范围级弱标签 Recall）和专家复核，不能单独报告误报率、特异度、完整混淆矩阵、ROC-AUC 或 PR-AUC。

---

## 11. 运行产物

```text
runs/{run_id}/
├─ run_config.yaml
├─ data_snapshot.json
├─ split_manifest.jsonl
├─ preprocess.json
├─ frequency_grid.npy             # 冻结的统一 Hz 坐标
├─ frequency_grid_metadata.json   # f_max、delta_f、点数和训练来源
├─ encoder.pt
├─ normal_reference.npz
├─ predictions_validation.parquet
├─ predictions_test.parquet
├─ metrics_validation.json
├─ metrics_test_abnormal_side.json
├─ rejected_samples.jsonl
└─ model_card.md
```

正式测试前冻结所有文件及哈希。任何参数、数据或切分变化必须生成新的 `run_id`。

---

## 12. 执行顺序

1. 冻结 6 个数据对象并生成 `data_snapshot_id`；新增对象为 `SD_DEV_38` 和 `SD_DEV_22`。
2. 生成单波形清单和范围清单。
3. 执行数据准入和统计检查。
4. 按第 3.2.1 节解析并校验 `SD_DEV_38`、`SD_DEV_22` 和 `SD_TEST_13` 的测点位置；22#直接扫描正常文件夹，38#按完整异常范围关联。
5. 使用固定 `split_seed`，分别对23#正常范围和22#正常文件夹以完整时间块随机划分约 70% 正常训练集和30%正常验证集；将38#完整异常范围全部分配到异常验证，检查对象、测点与RPM分箱覆盖并冻结切分清单。
6. 统计正常训练集每条波形的 `fs`、`N`、`N/fs`、`fs/N` 和 `fs/2.56`，确定并冻结每个机型的统一 Hz 网格。
7. 在原生 Hz 频率轴导出逐波形机理连续证据；另将普通谱和包络谱插值到冻结网格供频谱 CNN 使用。
8. 运行深度、机理两个单分支基线。
9. 按 3 个种子训练深度正常表征模型。
10. 使用正常验证和开发异常范围选择单分支或固定融合方案。
11. 冻结模型、融合权重、各正常参考组的 `evaluation_threshold` 和报告模板。
12. 运行半直驱跨机组正常+异常测试。
13. 双馈驱动端测试只生成域外探索报告。
14. 输出模型卡，状态最多标记为 `prototype_candidate`。

---

## 13. 执行前阻断项

以下任一项缺失时，对应实验停止：

1. 所有文件名均能按第 3.2.1 节解析；`SD_TEST_13` 正常与异常文件解析出的测点必须一致，并能匹配开发侧同名测点参考；
2. 已自动生成每个对象、测点、范围的文件数量、采样间隔、采样率和波形长度统计；同一测点长度不一致文件已剔除并登记；
3. 数据快照必须完整覆盖本文规定的正常来源：有明确正常范围的对象覆盖完整范围，`SD_DEV_22` 覆盖配置正常文件夹内的全部文件；
4. 波形与现有机理算法逐波形连续输出的唯一关联规则；
5. 测试测点是否存在匹配的开发正常参考；
6. 已配置并冻结 `SD_DEV_38` 异常文件目录，已按业务确认测点目录和文件名后缀统计实际测点、最早/最晚时间、采样率、波形长度和文件数，并确认全部纳入 2023-01-01 ～ 2025-09-15 完整异常范围；
7. 已配置并冻结 `SD_DEV_22` 正常文件夹路径，业务方已确认该文件夹整体为正常数据；已统计实际首末时间、测点和每个测点的原始数、通过数与拒绝数，不按时间范围截取；
8. 已确认 `SD_DEV_22` 四部件阶次与38#相同，可进入正式机理参考拟合；
9. 新增数据加入后已重新生成 `data_snapshot_id`、切分清单、频率网格和全部正常参考，旧运行产物不得覆盖或继续沿用。

另外，测试或生产波形的原生频谱必须覆盖冻结网格的最高频率；覆盖不足时拒绝当前模型输入，不能对缺失高频区间补 0。

新增数据完成准入后，半直驱正常建模可增加22#正常机组，异常验证可增加38#第二个独立故障事件，有助于缓解仅依赖23#单一开发机组和单一故障事件的问题。38#异常数据不得进入正常训练，22#正常数据不得作为独立外部测试；`SD_TEST_13` 继续保留为冻结后的跨机组正常+异常测试。当前数据仍不足以证明完整跨项目生产泛化能力，补充跨项目、同测点位置的正常测试范围后，才能正式评估跨项目外部误报能力。
