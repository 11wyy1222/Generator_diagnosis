# 模型修改记录：阶次频谱与竞争门控 v2

## 1. 状态与版本边界

- 状态：`EXPERIMENTAL / 当前未通过准入`，不得替换现有模型。
- 基线分支：`main`。
- v1 基线提交：`7e4af5a`。
- 实验分支：`experiment/order-spectrum-v2`。
- v1 默认配置仍为 `frequency_hz_v1 + spectrum_residual_v1`，旧模型权重可原样加载。
- v2 必须使用新的配置和新的 `run_dir`，禁止覆盖任何现有运行目录。
- 6 GB RTX A2000 上首次使用 `batch_size=32` 发生显存不足。随后用户将两个 v2 配置统一调整为
  `batch_size=8` 并完成三种子实验；其余训练配置保持冻结。编码器激活重计算仍启用。
- 由于 v2 与现有 v1 的 batch size 不同，本次不是严格的单变量结构消融。

## 2. 修改原因与证据限制

已有消融中，半直驱机理分支在13#、22#、38#上的表现明显优于频谱分支；原 gated
结构把频谱向量作为主干，把机理向量作为受门控残差，因此即使机理证据更可靠，也不能真正
在两者间择优。固定 Hz 频谱还会使相同机械阶次随转速漂移，增加跨转速学习难度。

这次结构调整受既有测试结果启发，因此13#、22#、38#及双馈F14都不再是完全未被观察的
无偏测试集。它们只能用于回归检查和描述性比较，不能单独作为 v2 上线证据。最终有效性仍需
新增、未参与设计和调参的跨项目正常+异常测试数据确认。

## 3. 修改内容

### 3.1 频谱分支

`shaft_order_v2` 仅使用当前波形 RPM 计算轴频 `RPM/60`，把普通谱和包络谱从 Hz 轴插值到
冻结的公共阶次轴。该步骤不读取外圈、内圈、滚动体或保持架理论故障阶次，因此仍保留发现
未知谱形的能力，并与机理分支保持职责区分。

输入为4通道：原始普通阶次谱、原始包络阶次谱、软抑制后的普通阶次谱、软抑制后的包络
阶次谱。默认只在1X、2X、3X附近各 `±0.08` 阶内线性衰减，中心保留25%；原始两通道始终
保留，不硬删除转频及其邻近边带。

### 3.2 竞争门控

`competitive_v2` 分别产生频谱专家和机理专家 logit，门控输出机理权重：

```text
w_mechanism = q_global * sigmoid(gate(z_spectrum, z_mechanism, q_global))
w_spectrum  = 1 - w_mechanism
logit       = w_spectrum * logit_spectrum + w_mechanism * logit_mechanism
```

当机理证据不可用（`q_global=0`）时自动退化为频谱专家；机理证据有效时，模型可在两个专家
之间连续择优，不再固定以频谱为主。内部日志新增两个专家概率及两分支权重。

## 4. 验证矩阵与准入规则

使用完全相同的冻结切分和种子比较：v1频谱、v2阶次频谱、v1 gated、v2竞争gated、
mechanism-only。模型选择只看开发验证集，不看13#/22#/38#/F14结果。

1. 先证明 v2 阶次频谱相对 v1 频谱在3个种子的验证集均值上有改善；否则不能把 gated 的变化
   归因于阶次对齐。
2. v2 gated 的验证集 PR-AUC、F1、正常特异度和混淆矩阵必须逐对象报告；至少不得以显著增加
   正常误报换取异常召回。
3. 报告3个种子的均值、标准差和最差种子，不允许只挑最好种子。
4. 检查 `spectrum_weight`、`mechanism_weight` 和两个专家概率。权重应随样本证据变化；若恒定
   塌缩，必须说明这是否与专家验证表现一致，不能仅凭最终准确率接受。
5. 13#/22#/38#只运行一次冻结阈值回归测试，不据此改阈值或继续调门控。
6. 进入生产前必须增加一批本次修改后才取得的跨项目、跨机组正常+异常盲测数据。

## 5. 训练与测试命令

以下命令均在 `D:\Generator_diagnosis` 执行。每个种子使用全新的运行目录。

```powershell
.\.venv\Scripts\python.exe -m bearing_diagnosis train `
  --config configs\semi_direct_order_spectrum_v2.yaml `
  --manifest dataset_root_semi_expanded\splits\weak_supervised_split.jsonl `
  --run-dir runs\semi_direct_order_spectrum_v2_seed2026 `
  --seed 2026 --device cuda

.\.venv\Scripts\python.exe -m bearing_diagnosis train `
  --config configs\semi_direct_order_gated_v2.yaml `
  --manifest dataset_root_semi_expanded\splits\weak_supervised_split.jsonl `
  --run-dir runs\semi_direct_order_gated_v2_seed2026 `
  --seed 2026 --device cuda

.\.venv\Scripts\python.exe -m bearing_diagnosis test `
  --run-dir runs\semi_direct_order_gated_v2_seed2026 `
  --manifest dataset_root_sd22_sd38_test\splits\weak_supervised_split.jsonl `
  --split new_turbine_test `
  --output-dir runs\semi_direct_order_gated_v2_seed2026\test_sd22_sd38 `
  --device cuda
```

将命令中的 `2026` 分别替换为 `2027`、`2028` 完成三种子实验。测试必须在验证选型和阈值
冻结之后执行。

## 6. 回滚

查看 v1 基线：

```powershell
git switch main
git show 7e4af5a --stat
```

实验未通过时直接保留实验分支和运行产物作审计，部署继续指向 `main` 与原 v1 `run_dir`。
不要覆盖旧权重。若未来已经把 v2 合并到主分支，应使用 `git revert <v2合并提交>` 生成可审计
的反向提交，而不是使用 `reset --hard` 改写历史。

## 7. 实验结果

2026、2027、2028三种子训练和13#、22#/38#冻结测试已经完成。order spectrum-only v2
跨种子高度不稳定；competitive gated v2 在13#的seed2027出现149条漏报，且不同种子学到
相反的专家路由语义。当前版本不通过准入，完整结果见
[`runs/semi_direct_order_v2_report_seeds2026_2028.md`](runs/semi_direct_order_v2_report_seeds2026_2028.md)。
