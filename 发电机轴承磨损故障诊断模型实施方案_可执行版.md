# 发电机轴承磨损故障诊断模型实施方案（可执行版）

## 1. 执行目标

本文档对应《发电机轴承磨损故障诊断算法设计》，用于直接指导数据整理、弱标签生成、训练、验证、测试和交付。

模型每次输入一个测点的一条完整波形，第一阶段只训练总体正常/异常二分类：

```json
{
  "sample_id": "sample_001",
  "abnormal_probability": 0.92,
  "component_probabilities": null
}
```

当前没有逐条专家确认标签。第一阶段采用时间范围标签传播：

```text
确认正常范围内[semi_direct_ablation_report_seeds2026_2028.md](runs/semi_direct_ablation_report_seeds2026_2028.md)的有效波形 → y_abnormal = 0
确认异常范围内的有效波形 → y_abnormal = 1
```

这些标签表示波形来自正常范围还是异常范围，属于弱监督标签；异常范围内并非每条波形都必然具有明显故障特征。

当前不训练外环、内环、滚动体和保持架分类头。已知故障部位只作为事件元数据、机理阶次配置和结果分组依据，不下放为逐波形部件标签。

---

## 2. 实际使用数据

### 2.1 数据对象

| 对象 ID | 机型 | 项目与机组 | 本模型用途 | 已知事件故障 | 异常范围 | 正常范围 | RPM |
|---|---|---|---|---|---|---|---|
| `SD_DEV_23` | 半直驱 | 阳江沙扒，23#，MySE6.45-180/105 | 弱监督训练+验证（正常+异常） | 驱动端轴承外环 | 2023-12-06 04:59:19 ～ 2024-01-06 04:59:19 | 2024-03-17 11:54:39 ～ 2024-04-17 11:54:39 | 180～350 |
| `SD_DEV_38` | 半直驱 | 阳江沙扒，38#，MySE6.45-180/105 | 新增异常弱监督训练+验证 | 非驱动端轴承外环 | 2023-01-01 ～ 2025-09-15（完整异常范围） | 无 | 180～350 |
| `SD_DEV_22` | 半直驱 | 阳江沙扒，22#，MySE6.45-180/105 | 新增正常弱监督训练+验证 | 无已知故障事件 | 无 | 正常文件夹内全部准入波形（不设时间范围） | 180～350 |
| `SD_TEST_13` | 半直驱 | 阳江沙扒，13#，MySE6.45-180/105 | 跨机组正常+异常测试 | 非驱动端轴承外环 | 2023-12-05 06:39:15 ～ 2024-01-05 06:39:15 | 2024-01-28 03:01:42 ～ 2024-02-28 03:01:42 | 180～350 |
| `DFIG_DEV_41` | 双馈 | 乳山半岛南海上风电 U 厂址一期，41#，MySE8.5-230/131 | 弱监督训练+验证 | 非驱动端轴承外环 | 2026-04-15 05:36:00 ～ 2026-05-15 05:36:00 | 2026-05-16 21:36:00 ～ 2026-06-16 21:36:00 | 600～1200 |
| `DFIG_TEST_F14` | 双馈 | 甘肃武威天祝，f14，MySE5.0-200/115 | 跨项目异常侧测试 | 驱动端轴承内环 | 2026-06-26 08:49:15 ～ 2026-07-26 08:49:15 | 无 | 1000～1900 |

半直驱 22#、23#、38# 和 13# 来自同一个项目。`SD_TEST_13` 相对于半直驱开发对象是同项目跨机组测试，不是跨项目测试。

除 `SD_DEV_38` 外，上表的异常范围是弱监督模型实际使用的**核心异常范围**，取《故障对象分类》中故障结束时间向前一个自然月。`SD_DEV_38` 按本次新增数据口径使用 2023-01-01 ～ 2025-09-15 的完整异常范围生成异常弱标签，不再截取结束日前一个月。各对象的原始完整异常时间范围继续保存在事件元数据中，用于追溯和故障演化分析。

`SD_DEV_38` 的 `fault_event_id` 单独记为 `SD_DEV_38_event_001`，与 `SD_DEV_23_event_001` 是两个独立故障事件。来源边界只给到日期，因此按自然日包含 2023-01-01 至 2025-09-15；清单构建时仍需记录范围内实际首条和末条有效波形时间。`SD_DEV_22` 不使用正常时间范围过滤：直接扫描配置的22#正常文件夹，文件夹内通过准入检查的全部波形生成正常弱标签；实际最早/最晚采集时间只写入数据快照统计，不作为纳入边界。

### 2.2 测点数量

- `SD_DEV_23`：4 个正式测点为 `12点`、`3点`、`6点`、`9点`，对应 4 个核心异常范围和 4 个正常范围。
- `SD_DEV_38`：实测新增文件名不含测点字段，因此 `sensor_position` 由业务确认的四个独立来源目录映射为 `12点/3点/6点/9点`；目录不得合并，每个实际出现的测点分别建立完整异常范围，但全部共享 `SD_DEV_38_event_001`。
- `SD_DEV_22`：新增正常数据的测点必须从文件名解析，取值限定为 `12点/3点/6点/9点`；每个实际出现的测点分别建立正常文件夹来源记录，不设时间范围。不得假设四个测点均存在，以准入后的清单统计为准。
- `SD_TEST_13`：具体测点从文件名解析，取值必须属于 `12点/3点/6点/9点`；同一测点的核心异常范围和维修后正常范围都进入跨机组测试，且两类文件的测点解析结果必须一致。
- `DFIG_DEV_41`：1 个非驱动端测点，对应 1 个异常范围和 1 个正常范围。
- `DFIG_TEST_F14`：1 个驱动端测点，只有 1 个异常范围。

半直驱同一机组 4 个测点范围可以产生多条训练样本，但共享同一 `fault_event_id` 时只计为 1 个独立故障事件。新增后半直驱开发侧最多有 2 个独立异常事件（23#、38#），不能按测点数扩大独立案例计数。

### 2.3 四部件特征阶次

| 对象 ID | `rolling_element` | `cage` | `outer_race` | `inner_race` |
|---|---:|---:|---:|---:|
| `SD_DEV_23` | 8.2638 | 0.4698 | 13.1560 | 14.8440 |
| `SD_DEV_38` | 5.3364 | 0.4536 | 12.6996 | 15.3004 |
| `SD_DEV_22` | 5.3364 | 0.4536 | 12.6996 | 15.3004 |
| `SD_TEST_13` | 5.3364 | 0.4536 | 12.6996 | 15.3004 |
| `DFIG_DEV_41` | 4.6100 | 0.4500 | 10.7100 | 13.2900 |
| `DFIG_TEST_F14` | 2.3590 | 0.3984 | 3.5856 | 5.4144 |

轴承型号名称仅作为可选追溯元数据，允许缺失，不作为数据准入条件、模型输入或机理计算依据。机理计算以本表及样本实际传入的四组特征阶次为准。

`SD_DEV_38` 的已知故障轴承型号为 `NU10/560M`，使用与表中一致的四组阶次。经业务确认，`SD_DEV_22` 的理论故障阶次与38#相同，因此22#可进入机理分支测试及后续正式训练；仍需在结果中按对象分别报告，避免22#正常与38#异常形成机组身份混杂。

```text
BPFO_hz = rpm / 60 × outer_race_order
BPFI_hz = rpm / 60 × inner_race_order
BSF_hz  = rpm / 60 × rolling_element_order
FTF_hz  = rpm / 60 × cage_order
```

理论频率及 1～5 阶谐波只用于峰值搜索，不直接输入机理 MLP。

---

## 3. 数据清单与标签

### 3.1 目录

```text
dataset_root/
├─ waveforms/
├─ manifests/
│  ├─ samples.jsonl
│  ├─ ranges.jsonl
│  ├─ rejected_samples.jsonl
│  └─ data_snapshot.json
├─ splits/
│  └─ weak_supervised_split.jsonl
└─ features/
   └─ mechanism_features.parquet
```

### 3.2 单波形记录

```json
{
  "sample_id": "SD_DEV_23_p03_20231206051000",
  "object_id": "SD_DEV_23",
  "project_id": "S1-20180011",
  "turbine_id": "23",
  "machine_type": "semi_direct",
  "sensor_position": "3点",
  "waveform_path": "[path]",
  "acquisition_time": "2023-12-06 05:10:00",
  "sampling_rate_hz": 25600.0,
  "waveform_length": 256000,
  "rpm": 215.0,
  "rpm_source": "filename",
  "range_id": "SD_DEV_23_p03_abnormal",
  "fault_event_id": "SD_DEV_23_event_001",
  "is_observed_scope_abnormal": true,
  "label_source": "abnormal_time_range",
  "component_orders": {
    "outer_race": 13.156,
    "inner_race": 14.844,
    "rolling_element": 8.2638,
    "cage": 0.4698
  },
  "component_labels": {
    "outer_race": null,
    "inner_race": null,
    "rolling_element": null,
    "cage": null
  }
}
```

#### 3.2.1 文件名解析与自动统计

去掉扩展名后，从文件名最右侧解析固定后缀：

```text
<设备与测点描述>_<fs>Hz_<rpm>_<YYYYMMDDhhmmss>
```

示例：

```text
广东能源尚洋湾海风场*23#*发电机3点径向_25600Hz_65.8824_20231004171053
```

对应：

```text
sensor_position = 3点
sampling_rate_hz = 25600
rpm = 65.8824
acquisition_time = 2023-10-04 17:10:53
```

实现时先按最右侧三个下划线字段解析 `fs`、RPM 和时间，再从前缀中的 `发电机(12点|3点|6点|9点)` 提取半直驱测点。文件名不匹配、时间非法、测点出现零次或多次时拒绝样本。`waveform_length=N` 为波形文件第一列的实际长度，不从文件名推断。

清单生成后，按 `object_id + sensor_position + range_id` 自动统计波形数量；按采集时间排序后计算相邻采样间隔，并报告最小值、中位数、P90、最大值、重复时间戳和明显断档。

### 3.3 标签生成规则

1. 正常范围内通过准入检查的每条波形标记 `is_observed_scope_abnormal=false`、`label_source=normal_time_range`。
   `SD_DEV_22` 为例外的文件夹确认正常来源：正常文件夹内通过准入检查的每条波形标记 `is_observed_scope_abnormal=false`、`label_source=confirmed_normal_directory`，不执行时间范围过滤。
2. 第 2.1 节异常范围内通过准入检查的每条波形标记 `is_observed_scope_abnormal=true`、`label_source=abnormal_time_range`；23#、13#、41#和F14使用表中核心异常范围，38#使用表中完整异常范围。
3. 不属于确认正常或异常范围、范围边界有争议或无法关联范围的波形不进入监督数据集。
4. 当前监督训练集总体标签只有 `true/false`，不使用 `abnormal_mask`。
5. 当前不增加 `confidence` 字段，所有范围传播标签等权进入第一阶段基线。
6. 异常范围不自动生成部件正标签，四个部件均填 `null`。
7. 确认正常范围可记录四个部件为 `false`，但第一阶段不计算部件损失。

已知事件为外环或内环故障，只用于事件级报告和机理阶次选择，不能据此认定异常范围内每条波形都有相应部件故障特征。

---

## 4. 数据准入

有效波形必须满足：

1. 文件存在且可读取为一维有限数值数组；
2. `waveform_length=N` 读取每条波形第一列长度；同一 `object_id + sensor_position` 只保留一致的目标长度，不一致文件从建模数据集剔除并写入 `rejected_samples.jsonl`，原始源文件不覆盖；
3. 采样率可解析且大于 0；
4. RPM 可解析并位于业务范围；
5. 数据点数能够执行 FFT；
6. 不存在严重削顶、饱和、恒定值或通信填充值；
7. 文件名解析出的时间、RPM、采样率和测点与对象配置、范围记录不存在冲突；
8. 四个 `component_orders` 为有限正数。

拒绝样本写入 `rejected_samples.jsonl`，不得静默跳过，不记录 `signal_quality_level`。

训练前必须统计每个对象、测点、范围、弱标签、RPM 分箱和时间块的样本数，并单独报告独立 `fault_event_id` 数量。

---

## 5. 数据切分

### 5.1 相对位置与时间块形成

对每个开发侧监督异常范围（38#为完整异常范围，其他对象为核心异常范围），先计算：

```text
range_progress = (acquisition_time - T_start) / (T_end - T_start)

early:  0   <= range_progress < 1/3
middle: 1/3 <= range_progress < 2/3
late:   2/3 <= range_progress <= 1
```

一个自然月范围中三段大致对应前、中、后各约 10 天；38#完整异常范围按其实际总时长三等分。`early/middle/late` 只表示当前监督异常范围内的相对时间位置，不表示真实故障等级，不作为模型输入。

时间块规则：

1. 当前监督异常范围在每个相对位置内默认按自然日形成 `sample_group_id`；
2. 正常范围不划分前、中、后，在完整范围内按自然日形成 `sample_group_id`；
3. 采样不规则时按连续采集批次分块；相邻时间差大于 `3 ×` 对应范围中位采样间隔时开始新块；
4. 不按单波形随机切分；同一原始波形及其内部窗口必须在同一时间块；
5. 监督异常范围的每个相对位置至少需要 3 个非空时间块；正常来源至少需要 3 个非空时间块；
6. 半直驱同一事件或同一正常范围中，同一自然日或同一采集批次的 4 个测点使用相同 `sample_group_id` 切分键，必须进入同一个数据子集。

### 5.2 训练与验证

冻结 `split_seed: 2026`，并在特征提取和训练前生成不可变切分清单：

1. 对监督异常范围，在 `early`、`middle`、`late` 各层内部随机分配约 70% 完整时间块到训练集、约 30% 到验证集；
2. 对正常来源，在各对象内部随机分配约 70% 完整时间块到训练集、约 30% 到验证集；`SD_DEV_22` 使用正常文件夹内全部准入波形；
3. 对含 `n_blocks` 个时间块的层，取 `n_val=max(1, round(0.3 × n_blocks))`，其余进入训练集，并保证至少有 2 个训练块；
4. 随机单位只能是 `sample_group_id`，禁止将同一时间块中的单波形拆到不同集合；
5. 半直驱同一日期或采集批次的 4 个测点共享切分结果，避免跨测点时间泄漏；
6. `SD_DEV_38` 的完整异常范围按 `early/middle/late` 分层后，以完整时间块约 70%/30% 划分训练和验证；`SD_DEV_22` 正常文件夹内的全部准入波形独立按完整时间块约 70%/30% 划分训练和验证；
7. 切分后必须按 `object_id + 弱标签 + sensor_position + rpm_bin` 报告覆盖。38#只有异常、22#只有正常，二者不能替代23#同一机组同时具有正常与异常范围的开发证据；
8. 所有模型结构、随机初始化种子和对比实验复用同一份 `split_manifest.jsonl`，不能为获得更好指标重复抽取切分。

`split_manifest.jsonl` 至少记录：

```json
{
  "sample_group_id": "SD_DEV_23_event_001_early_20231208",
  "range_id": "SD_DEV_23_p03_abnormal",
  "fault_event_id": "SD_DEV_23_event_001",
  "range_position": "early",
  "dataset_split": "train",
  "split_seed": 2026
}
```

正常时间块的 `range_position` 填 `null`。`range_position`、`dataset_split` 和 `split_seed` 只用于数据管理与复现，不作为模型输入。

这种验证是同项目内按机组、故障过程和完整时间块形成的开发验证，不是严格的未来时间外推验证，也不是独立故障事件留出验证。新增后必须分别报告23#与38#异常验证、23#与22#正常验证，再按独立 `fault_event_id` 等权汇总异常结果；不能把多个测点或大量38#波形当成等量独立事件。

### 5.3 测试

- `SD_TEST_13` 的核心异常范围和维修后正常范围整体进入跨机组测试，不进入训练或验证。
- `DFIG_TEST_F14` 的异常范围整体进入跨项目测试，不进入训练或验证。
- 测试范围只在网络结构、预处理、超参数、模型选择和 `evaluation_threshold` 冻结后运行。
- 双馈测试 1000～1200 RPM 记为 `in_range_test`；大于 1200 RPM 记为 `rpm_ood_test`，分别报告。

当前模型不输入 `sensor_position`，因此双馈可以执行跨测点位置异常侧测试，但必须将驱动端/非驱动端差异写入模型卡。

---

## 6. 共享信号预处理

### 6.1 时域

```text
完整波形 → 去均值 → 除以训练集绝对幅值全局 P99.5 → 时域编码器
```

禁止逐样本最大值归一化、标准化或总能量归一化。

### 6.2 普通谱和包络谱

```text
普通谱：去均值 → Hann 窗 → 单边 FFT → 线性幅值
包络谱：去均值 → 全频段 Hilbert 包络 → 去直流
        → Hann 窗 → 单边 FFT → 线性幅值
```

两者截取相同有效频带并插值到固定 Hz 网格，组成 `[2, frequency_bin_count]` 双通道输入。

第一版不使用带通滤波、对数幅值、阶次谱、角域重采样、谱峭度选带或逐样本能量归一化。

### 6.3 固定网格

当前各测点采样率 `fs` 接近，主要差异是采样点数 `N` 不一致。每条波形先按自身参数计算：

```text
duration_seconds = N / fs
source_delta_f_hz = fs / N
source_cutoff_hz = fs / 2.56
```

每个机型只使用**正常训练集**建立并冻结一套统一 Hz 网格：

```text
common_f_max_hz = min(
    business_f_max_hz,
    min(source_cutoff_hz of normal training samples)
)

common_delta_f_hz = max(
    source_delta_f_hz of normal training samples
)

common_frequency_axis = np.arange(
    0,
    common_f_max_hz + common_delta_f_hz / 2,
    common_delta_f_hz
)

frequency_bin_count = len(common_frequency_axis)
```

其中：

- `business_f_max_hz` 根据需要保留的轴承冲击、共振调制频带，以及 1～5 阶理论故障频率和边带确定；
- `common_delta_f_hz` 取正常训练数据中最大的真实 `fs/N`，避免把较短波形插值到虚假的更细频率分辨率；
- `frequency_bin_count` 由冻结后的频率上限和间隔自然确定；
- 网格冻结后，正常验证、异常验证、测试和生产必须加载同一份 `frequency_grid.npy`，不得重新计算。

网格充分性必须检查：

```text
common_f_max_hz >= 1.2 × 训练/验证对象的最大5阶理论故障频率
```

若不满足，应调整 `business_f_max_hz` 或确认采样率和现有 `fs/2.56` 截止规则能否覆盖目标频带，不能通过增加 `frequency_bin_count` 解决。

每条波形分别使用自身 `fs` 和 `N` 生成原生频率轴，截取到 `fs/2.56` 后，按真实 Hz 坐标线性插值到冻结网格。统一网格只服务频谱 CNN；四部件峰值、谐波、背景和边带等连续机理证据仍在插值前的原生 Hz 频率轴上提取。

### 6.4 RPM

```text
rpm_normalized = (rpm - rpm_min) / (rpm_max - rpm_min)
```

半直驱使用 180～350，双馈训练使用 600～1200。RPM 作为融合阶段标量，不设置独立深层编码器。

---

## 7. 机理特征

对外环、内环、滚动体和保持架分别计算 1～5 阶理论频率锚点：

```text
fft_resolution = sampling_rate_hz / waveform_length
search_half_width_hz = max(3 × fft_resolution, 0.05 × target_frequency_hz)
```

从普通谱和包络谱各提取一次：

- 各阶峰值幅值；
- 峰值/局部背景中位数比；
- 实际峰值相对理论位置偏差；
- 有效谐波数量、总和及衰减；
- 轴转频候选间隔的左右边带比和对称性；
- 对应 `valid_mask`。

理论频率及实际峰值的绝对 Hz 位置只用于搜索、频带覆盖和内部日志，不直接输入 MLP。

总体机理表示仅使用上述当前波形机理证据。现有布尔报警、`final_fault_level`、报警字符串和多波形汇总结果不得作为模型输入或训练标签。

---

## 8. 网络结构

分别训练：

- `weak_cls_semi_direct_v1`；
- `weak_cls_dfig_v1`。

### 8.1 谱形编码器

```yaml
time_encoder:
  type: multiscale_1d_cnn
  stem_channels: 32
  multiscale_kernels: [7, 15, 31]
  channels: [32, 64, 128]
  stride_per_block: 2
  padding: kernel_size_div_2_floor
  normalization: group_norm
  activation: gelu
  pool_segments: 8
  output_dim: 128

spectrum_encoder:
  input_channels: 2
  channels: [32, 64, 128]
  kernels: [9, 7, 5]
  stride_per_block: 2
  padding: kernel_size_div_2_floor
  normalization: group_norm
  activation: gelu
  pool_segments: 8
  output_dim: 128

spectrum_fusion:
  input: [z_raw, z_spectrum, rpm_normalized]
  hidden_dims: [128, 64]
  dropout: 0.2
  output_dim: 64
```

### 8.2 机理编码器与总体头

```yaml
shared_mechanism_mlp:
  hidden_dims: [64, 32]
  output_dim: 32

global_mechanism_mlp:
  hidden_dims: [64]
  output_dim: 64

abnormal_head:
  hidden_dims: [64, 32]
  output_dim: 1

mechanism_aux_head:
  hidden_dim: 32
  output_dim: 1
  external_output: false
```

四个候选部件共用机理 MLP，聚合得到 `z_phy_global`。第一阶段不启用四个部件分类头。

### 8.3 可靠度门控

```text
z_spec = SpectrumFusion([z_raw, z_spectrum, rpm_normalized])
z_phy_global = GlobalMechanismMLP(aggregate(z_phy[c]))
g_global = q_global × sigmoid(GlobalGate([z_spec, z_phy_global, q_global]))
gated_phy = g_global × Project(z_phy_global)
h_abnormal = LayerNorm(z_spec + gated_phy)
abnormal_probability = sigmoid(AbnormalHead([h_abnormal, z_spec × gated_phy]))
```

机理证据无效时 `q_global=0`，模型退化为仅谱形判断。

### 8.4 总体机理辅助头

```text
p_phy_aux = sigmoid(MechanismAuxHead(z_phy_global))
```

辅助头用于检查机理分支是否具有弱标签辨别能力，并防止主模型长期完全忽略机理分支。它不参与对外输出，也不保证机理分支对门控具有固定影响。

生产模型保留辅助头用于内部日志；对外仍只返回 `abnormal_probability`。

---

## 9. 损失与训练

### 9.1 损失

```text
L = BCEWithLogits(abnormal_logit, y_abnormal)
  + 0.10 × q_global × BCEWithLogits(mechanism_aux_logit, y_abnormal)
```

参数正则化由 AdamW 的 `weight_decay` 实现。

当前总体标签全部为 `true/false`，不使用 `abnormal_mask`。部件标签为 `null` 时不计算部件损失；第一阶段所有部件损失关闭。

### 9.2 采样

- 每个训练批次按正常/异常弱标签 1:1 采样。
- 半直驱对 4 个测点均衡采样。
- 半直驱新增对象进入训练后，在每个弱标签内部按 `object_id` 尽量均衡采样，避免38#异常数量或22#正常数量支配训练；同时监控模型是否学习到机组身份差异而非故障差异。
- 同一时间块每个 epoch 最多抽取相同数量样本，防止高密度时段支配训练。
- RPM 分箱在两类标签中尽量匹配；不能为了获得更好结果单独调整测试 RPM。

### 9.3 配置

```yaml
optimizer: AdamW
learning_rate: 0.001
weight_decay: 0.0001
batch_size: 32
max_epochs: 100
early_stopping_patience: 15
monitor: validation_pr_auc_range_label
gradient_clip_norm: 5.0
random_seeds: [2026, 2027, 2028]
```

---

## 10. 固定对比实验

所有实验使用同一数据快照、切分、训练轮次和随机种子：

1. A 组：仅谱形，`z_spec → AbnormalHead`；
2. B 组：仅机理，`z_phy_global → MechanismOnlyHead`；
3. C 组：直接拼接，`[z_spec, z_phy_global] → AbnormalHead`；
4. D 组：可靠度门控融合。

同时记录：

- `q_global`、`g_global`、`g_global/q_global`；
- 门控后机理/谱形向量范数比；
- 机理辅助概率；
- 融合前后异常 logit 差；
- 机理缺失时退化比例；
- 高可靠度但门值接近 0 的样本比例。

只有 D 组在正常和异常验证时间块上稳定优于 A、C 组，且没有明显增加正常弱标签误报时，门控结构才保留为候选方案。测试集不参与四组选型。

---

## 11. 评估

### 11.1 开发验证

按范围传播弱标签计算：

- Accuracy、Balanced Accuracy；
- PR-AUC、ROC-AUC；
- Precision、Recall、F1；
- 混淆矩阵；
- 正常/异常概率分布；
- 每个机型、测点、RPM 分箱和 `range_id` 的结果；
- 监督异常验证集按 `early`、`middle`、`late` 分别报告异常范围阈值命中率（弱标签 Recall）以及 `abnormal_probability` 的 P50、P90；38#使用完整异常范围的三等分；
- 3 个随机种子的均值和标准差。

阈值型指标的执行口径：

```text
evaluation_threshold = 开发验证集上使 F1 最大的阈值
y_pred = 1, if abnormal_probability >= evaluation_threshold, else 0

Accuracy = (TP + TN) / (TP + TN + FP + FN)
Balanced Accuracy = 0.5 × [TP / (TP + FN) + TN / (TN + FP)]
异常范围阈值命中率 = 异常范围内 abnormal_probability >= evaluation_threshold 的有效波形数 / 异常范围有效波形总数
```

异常范围阈值命中率等同于按范围传播弱标签计算的 Recall，因此简称“弱标签 Recall”；它不能解释为基于逐波形人工真值的真实 Recall。候选模型选定后，将其 `evaluation_threshold` 连同模型和预处理参数一起写入本次运行产物并冻结。外部测试只能复用该阈值，禁止根据测试 Accuracy 重新调阈值。该阈值仅用于离线评估，不写入对外单波形输出，也不是生产告警阈值。

这些指标只表示模型区分“来自正常时间范围的波形”和“来自异常时间范围的波形”的能力，不能解释为逐波形真实诊断性能。

### 11.2 外部测试

统一报告：

- 有效波形数和拒绝数；
- `abnormal_probability` 的 P10、P25、P50、P75、P90；
- 相对开发正常验证分布的位置；
- 使用开发阶段冻结的 `evaluation_threshold` 计算异常范围阈值命中率（弱标签 Recall）；
- Top-N 波形及专家复核结果；
- 核心异常范围按 `early`、`middle`、`late` 分别报告分数分布和异常范围阈值命中率（弱标签 Recall）；
- RPM 是否属于训练范围；
- 项目、机型型号、测点位置和已知故障部位差异。

`SD_TEST_13` 同时具有正常范围和核心异常范围，可复用冻结的 `evaluation_threshold`，按范围传播弱标签报告 Accuracy、Balanced Accuracy、异常范围阈值命中率（弱标签 Recall）、特异度、误报率、混淆矩阵、ROC-AUC 和 PR-AUC，但只能解释为跨机组正常/异常范围来源可分性，不能解释为逐波形真实诊断性能。

`DFIG_TEST_F14` 仍只有核心异常范围，只能报告异常范围阈值命中率（弱标签 Recall）等异常侧结果，不能单独报告 Accuracy、Balanced Accuracy、特异度、误报率、完整混淆矩阵、ROC-AUC 或 PR-AUC；弱标签 Recall 不能解释为逐波形真实 Recall。

`DFIG_TEST_F14` 中 1000～1200 RPM 和大于 1200 RPM 必须分别报告。

---

## 12. 输出与内部日志

### 12.1 对外输出

```json
{
  "sample_id": "sample_001",
  "abnormal_probability": 0.92,
  "component_probabilities": null
}
```

`abnormal_probability` 是针对范围传播弱标签训练得到的 Sigmoid 分数，不是经过逐波形真值校准的故障概率。模型不输出阈值判断、业务结论或 `confidence`。

### 12.2 内部日志

保存：

- 模型版本、数据快照和配置哈希；
- 对象、项目、机组、测点、时间、RPM；
- 固定频带和 FFT 分辨率；
- 四部件理论频率、1～5 阶覆盖和连续机理证据；
- `valid_mask`、`q_global`、`g_global`；
- 机理辅助概率；
- 门控融合前后 logit 差；
- 输入拒绝、机理缺失和模型降级原因。

---

## 13. 运行产物

```text
runs/{run_id}/
├─ run_config.yaml
├─ data_snapshot.json
├─ split_manifest.jsonl
├─ preprocess.json
├─ frequency_grid.npy
├─ frequency_grid_metadata.json
├─ model.pt
├─ predictions_validation.parquet
├─ predictions_test.parquet
├─ gate_monitoring.parquet
├─ metrics_validation.json
├─ metrics_test_abnormal_side.json
├─ rejected_samples.jsonl
└─ model_card.md
```

正式测试前冻结全部文件及哈希。任何配置、数据或切分变化必须生成新 `run_id`。

---

## 14. 执行顺序

1. 冻结 6 个对象的原始数据快照；新增对象为 `SD_DEV_38` 和 `SD_DEV_22`。
2. 生成范围/来源清单：异常对象按时间范围关联，`SD_DEV_22` 直接扫描正常文件夹并生成 `confirmed_normal_directory` 来源记录。
3. 执行准入检查和数据统计。
4. 将监督异常范围标记为 `early/middle/late`（38#使用完整异常范围），使用固定 `split_seed` 生成不可变的分层时间块训练/验证切分；正常来源按完整时间块随机切分。
5. 只使用正常训练集的 `fs`、`N`、`fs/N` 和 `fs/2.56` 确定并冻结每个机型的固定 Hz 网格；其他预处理参数按各自训练规则拟合。
6. 导出逐波形四部件连续机理特征和有效掩码。
7. 运行现有 FFT 机理规则基线。
8. 按 3 个种子训练 A、B、C、D 四组模型。
9. 使用开发验证集选择谱形结构和机理融合方式。
10. 冻结模型、`evaluation_threshold` 和报告模板。
11. 运行 `SD_TEST_13` 跨机组正常+异常测试。
12. 分别运行 `DFIG_TEST_F14` 的 `in_range_test` 和 `rpm_ood_test`。
13. 输出模型卡，明确弱标签含义以及 `DFIG_TEST_F14` 的异常-only 测试限制。

---

## 15. 执行前阻断项

缺失以下任一项时，对应实验停止：

1. 所有文件名均能按第 3.2.1 节解析；`SD_TEST_13` 正常与异常文件解析出的测点必须一致，并能匹配开发侧同名测点；
2. 已自动生成每个对象、测点、范围的文件数量、采样间隔、采样率和固定波形长度统计；长度不一致文件已剔除并登记；
3. 数据快照必须完整覆盖本文规定的正常来源：有明确正常范围的对象覆盖完整范围，`SD_DEV_22` 覆盖配置正常文件夹内的全部文件；
4. 波形路径、采集时间、RPM 与当前波形机理结果的唯一关联规则；
5. 已从 `SD_DEV_38` 的业务确认测点目录和文件名后缀统计实际测点、最早/最晚时间、采样率、波形长度和文件数，并确认全部纳入 2023-01-01 ～ 2025-09-15 完整异常范围；
6. 已配置并冻结 `SD_DEV_22` 正常文件夹路径，业务方已确认该文件夹整体为正常数据；已自动统计实际最早/最晚时间、测点和每个测点的原始数、通过数与拒绝数，不按时间范围截取；
7. 已确认 `SD_DEV_22` 四部件阶次与38#相同，可进入正式机理融合训练；
8. 新增数据加入后已重新生成 `data_snapshot_id` 和不可变切分，旧运行目录不得覆盖或继续沿用。

新增数据完成准入后，半直驱开发侧可增加一个独立异常事件（38#）和一个正常机组（22#），有助于降低仅依赖23#单一开发机组的风险。但38#只有异常、22#只有正常，仍存在机组身份与弱标签相关的混杂风险；必须保留13#跨机组正常+异常测试，并按对象报告结果。当前数据仍不能证明逐波形真实分类能力，也不能训练可靠的四部件定位模型。补充跨项目正常测试范围、同机组正常/异常成对数据和更多独立故障事件后，才能进入生产有效性验证。
