# 训练采样器修复记录

## 修改目标

本次只修改训练数据调度与训练损失，不修改 v1 模型网络结构、推理接口、验证集或测试集逻辑。

原 `BalancedGroupBatchSampler` 会按最小时间组配额、正常/异常1:1和异常阶段均衡进行截断，
导致大量训练记录在一个 epoch 中没有进入优化。新实现取消这种下采样。

## 新训练规则

1. 每个 epoch 使用训练清单中的全部真实记录，每条真实记录恰好贡献一次损失。
2. 仍按 `waveform_length` 分桶，避免不同长度波形进入同一 batch。
3. 桶内样本以 `seed + epoch` 确定性打乱，批次顺序也确定性打乱。
4. 所有训练 batch 均严格等于配置的 `batch_size`。
5. 每个长度桶最后一个 batch 不足时，从同桶复制样本补齐；补齐项的损失权重为0，
   只用于保持张量形状，不贡献主损失或机理辅助损失。
6. 对固定数据快照，优化步数固定为：

```text
steps_per_epoch = sum(ceil(length_bucket_size / batch_size))
```

## 分组/类别加权损失

训练样本权重分两级计算：

- 正常与异常类别的总权重相等；
- 每个类别内部，`object_id + sample_group_id` 各时间组的总权重相等；
- 同一时间组内部的样本均分该组权重；
- 最后将全体样本权重归一化为均值1。

主异常损失和机理辅助损失均使用该权重。验证和测试不使用训练权重，仍按每条记录等权统计。

## 实际清单核对

| 机型 | 训练记录 | 正常/异常 | batch size | 固定步数/epoch | 零权重补齐 | 实际覆盖 |
|---|---:|---:|---:|---:|---:|---:|
| 半直驱扩展清单 | 912 | 60 / 852 | 32 | 29 | 16 | 912/912 |
| 双馈扩展清单 | 2243 | 94 / 2149 | 8 | 281 | 5 | 2243/2243 |

两份清单的正常、异常损失权重总量分别严格等分为总样本权重的一半。

## 运行产物

新训练运行会增加 `training_sampling.json`，记录：

- 采样策略；
- 训练样本数；
- batch size；
- 每轮固定优化步数；
- 每轮零权重补齐数；
- 样本权重最小值、最大值和均值。

`training_history.json` 的每个 epoch 同时记录 `optimization_steps` 和
`training_samples_covered`，用于检查训练过程没有退回旧的下采样行为。

## 兼容性

保留 `BalancedGroupBatchSampler` 名称作为导入兼容别名，但其行为已经改为全覆盖，不再执行
均衡下采样。旧模型 checkpoint 和推理行为不受影响；使用新采样器后训练得到的新模型必须使用
新的运行目录和版本标识，不得覆盖旧训练产物。
